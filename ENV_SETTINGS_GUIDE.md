# Руководство по настройке .env файла

## ⚠️ ВАЖНО: Что НЕ нужно менять

Следующие параметры уже настроены оптимально после последних изменений. **Не меняйте их**, если не понимаете последствий:

### Основные параметры (уже настроены)
- `RSI_LONG_MIN=45` - минимум RSI для LONG (было 48)
- `RSI_LONG_MAX=62` - максимум RSI для LONG (было 58)
- `RSI_SHORT_MIN=38` - минимум RSI для SHORT (было 42)
- `RSI_SHORT_MAX=55` - максимум RSI для SHORT (было 52)
- `VOL_SPIKE_MULTIPLIER=1.5` - множитель всплеска объема (было 1.8)
- `MIN_ADX=20.0` - минимальный ADX (было 25.0)
- `MIN_RECENT_CHANGE_PCT=0.3` - минимальное изменение за последние свечи (было 0.4)

### Опциональные проверки (отключены по умолчанию)
- `RECENT_MOVE_CHECK=0` - проверка недавнего движения (было 1)
- `RSI_ENTRY_CHECK=0` - проверка входа RSI (было 1)
- `EMA_CROSS_RECENT=0` - проверка недавнего пересечения EMA (было 1)
- `VOL_RECENT_CHECK=0` - проверка недавнего объема (было 1)

**Эти параметры уже настроены для получения ~5 сигналов в сутки. Не меняйте их без необходимости.**

---

## ✅ Что МОЖНО безопасно менять

### 1. Количество сигналов в день
```env
MAX_SIGNALS_PER_DAY=5
```
- **Рекомендуемые значения:** 3-10
- **Что делает:** Ограничивает количество лучших сигналов в день
- **Безопасно менять:** Да, если хотите больше/меньше сигналов

### 2. Количество топ-муверов для сканирования
```env
TOP_N=8
```
- **Рекомендуемые значения:** 5-15
- **Что делает:** Сколько растущих/падающих монет сканировать
- **Безопасно менять:** Да, больше = больше потенциальных сигналов

### 3. Интервал сканирования
```env
SCAN_INTERVAL_SECONDS=600
```
- **Рекомендуемые значения:** 300-1800 (5-30 минут)
- **Что делает:** Как часто бот сканирует рынок
- **Безопасно менять:** Да, но не ставьте меньше 60 секунд

### 4. Минимальный объем 24ч
```env
MIN_QUOTE_VOLUME_USDT=5000000
```
- **Рекомендуемые значения:** 1000000-10000000 (1M-10M USDT)
- **Что делает:** Фильтрует монеты с низкой ликвидностью
- **Безопасно менять:** Да, меньше = больше монет для сканирования

### 5. Максимальное изменение за 24ч
```env
MAX_24H_CHANGE=20.0
USE_MAX_24H_FILTER=1
```
- **Рекомендуемые значения:** 15.0-30.0
- **Что делает:** Исключает перекупленные/перепроданные монеты
- **Безопасно менять:** Да, больше = больше монет для сканирования

### 6. Управление риском
```env
BYBIT_RISK_PERCENT=1.0
```
- **Рекомендуемые значения:** 0.5-2.0
- **Что делает:** Процент баланса на риск в одной сделке
- **Безопасно менять:** Да, но будьте осторожны с большими значениями

### 7. Stop Loss / Take Profit множители
```env
ATR_SL_MULTIPLIER=1.2
ATR_TP1_MULTIPLIER=2.5
ATR_TP2_MULTIPLIER=4.0
```
- **Рекомендуемые значения:** 
  - SL: 1.0-2.0 (меньше = уже стоп-лосс)
  - TP1: 2.0-3.5 (больше = дальше первый тейк-профит)
  - TP2: 3.0-5.0 (больше = дальше второй тейк-профит)
- **Что делает:** Определяет расстояние SL/TP от входа
- **Безопасно менять:** Да, но проверьте Risk/Reward соотношение

### 8. Фильтр по тренду BTC
```env
BTC_TREND_FILTER=1
```
- **Значения:** 0 или 1
- **Что делает:** Блокирует LONG при падении BTC и SHORT при росте BTC
- **Безопасно менять:** Да, 0 = отключить фильтр (больше сигналов, но рискованнее)

### 9. Использование MACD и ADX
```env
USE_MACD=1
USE_ADX=1
```
- **Значения:** 0 или 1
- **Что делает:** Использует MACD/ADX для бонусов в score (не блокирует сигналы)
- **Безопасно менять:** Да, 0 = отключить (не повлияет на количество сигналов, только на score)

---

## 🔧 Если нужно БОЛЬШЕ сигналов

Если все еще мало сигналов, можно ослабить:

```env
# Расширить зоны RSI еще больше
RSI_LONG_MIN=42
RSI_LONG_MAX=65
RSI_SHORT_MIN=35
RSI_SHORT_MAX=58

# Снизить требования к объему
VOL_SPIKE_MULTIPLIER=1.3

# Увеличить количество сканируемых монет
TOP_N=12

# Отключить фильтр BTC (рискованнее!)
BTC_TREND_FILTER=0
```

---

## 🔒 Если нужно МЕНЬШЕ, но КАЧЕСТВЕННЕЕ сигналов

Если слишком много сигналов или хотите строже фильтрацию:

```env
# Ужесточить зоны RSI
RSI_LONG_MIN=48
RSI_LONG_MAX=58
RSI_SHORT_MIN=42
RSI_SHORT_MAX=52

# Увеличить требования к объему
VOL_SPIKE_MULTIPLIER=1.8

# Включить дополнительные проверки
RECENT_MOVE_CHECK=1
RSI_ENTRY_CHECK=1
EMA_CROSS_RECENT=1
VOL_RECENT_CHECK=1

# Увеличить минимальный ADX
MIN_ADX=25.0
```

---

## 📋 Пример минимального .env файла

```env
# Обязательные параметры
TELEGRAM_BOT_TOKEN=your_token_here
TELEGRAM_CHAT_ID=your_chat_id_here

# Биржа
EXCHANGE=bybit
BYBIT_FUTURES_BASE=https://api-demo.bybit.com
BYBIT_API_KEY=your_api_key
BYBIT_API_SECRET=your_api_secret
BYBIT_ENABLE_TRADING=1
BYBIT_RISK_PERCENT=1.0

# Основные настройки (можно не указывать, будут значения по умолчанию)
TOP_N=8
SCAN_INTERVAL_SECONDS=600
MAX_SIGNALS_PER_DAY=5
```

---

## ⚠️ ПРЕДУПРЕЖДЕНИЯ

1. **Не меняйте параметры RSI, VOL_SPIKE_MULTIPLIER, MIN_ADX** без понимания последствий - они уже оптимизированы
2. **Не ставьте BYBIT_RISK_PERCENT > 2.0** - это опасно для депозита
3. **Не отключайте BTC_TREND_FILTER** без понимания рисков
4. **Всегда тестируйте изменения** на demo/testnet перед использованием на реальных деньгах

---

## 📊 Текущие оптимальные значения (после последних изменений)

Эти значения уже установлены в коде и работают оптимально для ~5 сигналов в сутки:

- RSI зоны: LONG 45-62, SHORT 38-55
- VOL_SPIKE_MULTIPLIER: 1.5
- MIN_ADX: 20.0
- MIN_RECENT_CHANGE_PCT: 0.3
- Все опциональные проверки: отключены (0)
- Минимальный score: 45.0 (в коде, не в .env)

**Не нужно ничего менять, если все работает нормально!**
